# 라이선스 검증 (docs/license-supabase.md 참고)
# 이 파일을 .env 로 복사 후 실 값을 채우세요. .env 는 .gitignore 됨.

# Supabase 프로젝트 URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon key (public — main process 만 사용. renderer 번들에 안 들어감)
SUPABASE_ANON_KEY=eyJhbGciOi...
