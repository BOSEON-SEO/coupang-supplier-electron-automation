## 1.0.16 — 긴급 수정 (API 토큰 401 무한반복)

### 버그
- 1.0.7 의 secrets store 도입 후 plugin 의 API 호출이 토큰을 못 가져와 모든 요청이 401 → 달력 원격 조회/Eflex/회사정보 등 다 실패. apiToken 룩업 함수가 settings.json 만 보고 secrets.enc 를 무시. 호출부 13개 모두 secrets 합산 헬퍼로 교체
