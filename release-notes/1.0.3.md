## 1.0.3 — 긴급 수정 #2

### 버그
- 패키지 빌드에서 PO 다운로드 등 모든 Python 자동화가 ENOENT(-4058) 로 실패. asar 가상 fs 안의 경로를 child_process.spawn 에 그대로 넘겨 OS 가 못 찾던 문제. `app.asar` → `app.asar.unpacked` 변환

또한 사용자 PC 에 시스템 Python 이 깔려있어야 함 — venv 는 빌드에 포함되지 않음.
