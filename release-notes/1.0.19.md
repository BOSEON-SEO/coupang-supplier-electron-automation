## 1.0.19 — 쿠팡반출 v3 (9컬럼) 양식 전환

### 변경
- 쿠팡반출 양식을 **9컬럼 단일 시트** (v3) 로 전환:
  `물류센터 | 발주번호 | SKU Barcode | 확정수량 | 운송방법 | 박스번호 | 송장번호 | 파렛트번호 | 비고`
  - 제거: 총 주문수량 / 총 풀필반출 / 상품코드 / 상품명 / 신청수량 / 반출 / 창고수량
  - 발주번호 · SKU Barcode 셀 텍스트 양식(`@`) — 앞자리 0 보존
  - 쉽먼트 행 파레트번호=0, 밀크런 행 송장번호=빈 셀
- apply 매칭 키 `orderSeq|wh|bc` (상품코드 미사용). 발주번호 누락 시 last-seen carry-forward → (wh,bc) 단일 PO 자동 추정 → 없으면 skip 의 3단 fallback. 구버전 16컬럼 양식도 호환.
- 플러그인 v1.2.6 동시 릴리즈
  - TbnwsCoupangExportModal 안내 9컬럼으로 갱신
  - 🗑 설정 초기화 버튼

### 의존성
- `coupang-supplier-plugin-tbnws` 1.2.5 → 1.2.6
